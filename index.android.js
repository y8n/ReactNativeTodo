/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import {
    AppRegistry
} from 'react-native';
import ReactNativeTodoApp from './app';

AppRegistry.registerComponent('ReactNativeTodo', () => ReactNativeTodoApp);